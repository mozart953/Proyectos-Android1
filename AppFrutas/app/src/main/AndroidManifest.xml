<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appfrutas">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppFrutas">

        <activity
            android:name=".MainActivity2_Nivel1"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity_Nivel2"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity_Nivel3"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity2_Nivel4"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity2_Nivel5"
            android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".MainActivity_Nivel6"
            android:configChanges="orientation|screenSize"/>
        <activity android:name=".MainActivity"
            android:configChanges="orientation|screenSize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>